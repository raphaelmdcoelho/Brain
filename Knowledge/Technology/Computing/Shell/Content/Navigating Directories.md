
```bash
ls -a|--all
ls -l # long form
ls -l -h|--human-readable # long form with human readable
stat foo.txt
tree
tree -a
cd ~ # home
cd # home
cd / # root
cd - # last directory
pushd foo # go to foo sub-directory and add previous page to stack
popd # go back to directory in stack saved by pushd
```

#technology #computing #shell 