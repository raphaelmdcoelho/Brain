```python
names = ['John', 'Bob', 'Mosh']
names[0]
names[-1] # last item
names[2:] # range of item starting from the index 2
```

### 2D list

```python
matrix = [
	[1,2,3],
	[4,5,6],
	[7,8,9]		
]

matrix[0][1] # output: 2
```

### List methods

```python

numbers = [1,2,3,4,5,6,7,8]
numbers.append(9)
numbers.insert(2, 10)
numbers.remove(4)
numbers.clear()
numbers.pop()
numbers.index(3)
10 in numbers
numbers.count(7)
numbers.sort()
numbers.reverse()
numbers.copy()

numbers = [1,1,2,2,3,3,4,4]
unique = []
for number in numbers:
	if number not is uniques:
		uniques.append(number)
```

#python